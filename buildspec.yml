version: 0.2

phases:
	install:
		runtime-versions:
			openjdk: 8
		commands:
			- echo "installing iniziale"
	pre_build:
		commands:
			- echo "Questa è la buil phase"
	build:
		commands:
			- echo "siamo nella build block"
	post_build:
		commands:
			- echo "siamo nella post build"
		
